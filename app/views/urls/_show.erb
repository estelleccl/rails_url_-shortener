<section id="portfolio">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <h2>Your Shorten Url:</h2>
        <hr class="star-primary">
        <p><%= @url.long_url %>-<%= @url.short_url%></p>
      </div>
    </div>
  </div>
</section>