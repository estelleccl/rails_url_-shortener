<section id="portfolio">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <h2>Your Shorten Url:</h2>
        <hr class="star-primary">
        <p><b>Your Long Url:</b> - <%= @url.long_url %></p>
        <p id="shorten-url"><b>Shorten Url:</b> - <%= link_to "#{request.base_url}/#{@url.short_url}", "#{request.base_url}/#{@url.short_url}" %></p>
      </div>
    </div>
  </div>
</section>